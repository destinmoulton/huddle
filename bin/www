#!/usr/bin/env node
var app = require('../dist/server/server');

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function(){
    console.log('Huddle server listening on port '+server.address().port);
});